apiVersion: v1
kind: Service
metadata:
  name: sb-ecom-service
spec:
  type: LoadBalancer   # 🔥 changed from NodePort
  selector:
    app: sb-ecom
  ports:
    - port: 80
      targetPort: 9090
      protocol: TCP